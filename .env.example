# Free key from https://www.eia.gov/opendata/register.php
EIA_API_KEY=

# Optional server-side endpoint returning the normalized tanker JSON contract in
# docs/API.md. If omitted, the map uses a timestamped public latest-available feed.
# Commercial AIS providers can be adapted without exposing keys.
TANKER_API_URL=
TANKER_API_KEY=

# Optional JSON polling feed matching the contract in docs/API.md.
POLLING_API_URL=
